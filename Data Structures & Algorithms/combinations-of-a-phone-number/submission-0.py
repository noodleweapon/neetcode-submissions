class Solution:
    def rec(self, digits, cur, res):
        m = {
            "2": list("abc"),
            "2": list("abc"),
            "2": list("abc"),
        }

    def letterCombinations(self, digits: str) -> List[str]:
        cur, res = [], []
        self.rec(digits, cur, res)